

.custom-popup {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.871);
  z-index: 9999; /* Ajustado o valor do z-index para garantir que o popup esteja acima de outros elementos */
}

.custom-popup-content {
  
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: var(--brand-beige);
  padding: 20px;
  border-radius: 8px;
  text-align: center;
  z-index: 10000; /* Ajustado o valor do z-index para garantir que o conteúdo do popup esteja acima do fundo */
}

.close-btn {
  position: absolute;
  top: 10px;
  right: 10px;
  cursor: pointer;
  font-size: 3rem;
  color: var(--brand-beige);
  z-index: 10001; /* Ajustado o valor do z-index para garantir que o botão de fechar esteja acima do conteúdo do popup */
}

#services-interno .card circle {
  fill: var(--orange-asp);
}

#services-interno .card p {
  text-align: justify;
}

#services-interno header h2 {
  margin-bottom: 1rem;
}

#services-interno .cards {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap;
  min-width: 95vh;
  gap: 3.2rem;
}

#services-interno .card {
  padding: 1.4rem;
  text-align: center;

  background: white;

  border: 1px solid var(--orange-asp);
  border-radius: 0.6rem;
}

#services-interno .card h3 {
  margin-block: 1.6rem;
}

